# Python deps for the LiDAR-constrained refinement.
# Verified working set: pycolmap 4.0.4 / pyceres 2.6 (see refine.py header).

pycolmap>=4.0      # read/write COLMAP model + CeresBundleAdjuster (reprojection + manifolds)
pyceres>=2.6       # Ceres bindings: Problem, custom CostFunction, manifolds, solve
numpy>=1.24
scipy>=1.10        # cKDTree plane lookup + matrix->quat in prealign
open3d>=0.17       # point-cloud load, voxel downsample, scaled ICP
laspy[lazrs]>=2.5  # streaming chunked LAS/LAZ read for out-of-core crop of the fat cloud
pye57>=0.4         # read .e57 scans (lazy-imported in lidar_index + scripts/e57_to_laz.py)
pyyaml>=6.0
